{
  "name": "vault",
  "version": "2.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-about.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "marked": "^18.0.6",
    "vitest": "^4.1.10"
  }
}
